
  # Food Ordering App

  This is a code bundle for Food Ordering App. The original project is available at https://www.figma.com/design/lb1AOudTiC4w5tgldaOlxm/Food-Ordering-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  